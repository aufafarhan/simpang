import type { Metadata } from "next";
import Icon from "@/components/ui/Icon";
import Breadcrumb from "@/components/ui/Breadcrumb";
import FormPengaduan from "@/components/pengaduan/FormPengaduan";
import { getProfilDesa } from "@/lib/api";

export const metadata: Metadata = { title: "Pengaduan & Aspirasi", description: "Sampaikan laporan, keluhan, atau saran untuk Nagari Simpang." };
export default async function PengaduanPage() { const profil = await getProfilDesa(); const kontak = [{ icon: "location_on", judul: `Kantor ${profil.sebutan_desa}`, isi: profil.alamat }, { icon: "phone", judul: "Telepon", isi: profil.telepon || "Hubungi kantor Nagari" }, { icon: "mail", judul: "Email", isi: profil.email }]; return <main className="mx-auto w-full max-w-6xl px-4 py-8 md:px-6 md:py-12"><Breadcrumb items={[{ label: "Beranda", href: "/" }, { label: "Pengaduan & Aspirasi" }]} /><header className="mx-auto mb-10 max-w-3xl text-center"><h1 className="mb-3 text-4xl font-bold text-primary md:text-5xl">Pengaduan & Aspirasi</h1><p className="text-base leading-relaxed text-on-surface-variant md:text-lg">Sampaikan laporan, keluhan, atau saran Anda untuk kemajuan Nagari Simpang. Suara Anda sangat berarti bagi pembangunan komunitas kita.</p></header><div className="grid gap-6 lg:grid-cols-12"><aside className="space-y-5 lg:col-span-4"><section className="rounded-xl border border-outline-variant bg-surface-container-low p-6 shadow-level1"><h2 className="mb-5 inline-block border-b-2 border-secondary-container pb-2 text-xl font-bold text-primary">Informasi kontak</h2>{kontak.map((item) => <div key={item.judul} className="mb-5 flex gap-3 last:mb-0"><Icon name={item.icon} filled size={21} className="mt-0.5 shrink-0 text-secondary" /><div><h3 className="text-sm font-bold text-on-surface">{item.judul}</h3><p className="mt-1 whitespace-pre-line text-sm leading-relaxed text-on-surface-variant">{item.isi}</p></div></div>)}</section><section className="rounded-xl border border-outline-variant bg-primary p-6 text-on-primary"><Icon name="info" size={24} /><h2 className="mt-3 text-xl font-bold">Butuh bantuan cepat?</h2><p className="mt-2 text-sm leading-relaxed text-on-primary-container">Untuk keadaan darurat, segera hubungi layanan terkait atau kantor Nagari.</p></section></aside><div className="lg:col-span-8"><FormPengaduan /></div></div></main>; }
